export declare class TemplateRenderer {
    private static readonly templatesPath;
    private overwrite;
    render(templateName: string, context: any, outFile?: string | undefined): boolean;
}
